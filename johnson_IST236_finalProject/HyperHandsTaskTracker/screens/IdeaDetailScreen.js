import * as util from '../index';
import { Colors, Fonts, styles } from '../constants/index';
export const IdeaDetailScreen = () => {
  return (
    <util.View style={styles.container}>
      <util.util.Text style={styles.title}>IdeaDetailScreen</util.util.Text>
    </util.View>
  );
}